name: CI
# Checks linting, formatting, and tests.

on:
  pull_request:
  workflow_dispatch:

jobs:
  ci:
    uses: luamod/.github/.github/workflows/ci.yml@v1
    with:
      manual_run: ${{ github.event_name == 'workflow_dispatch' }}
      lua_versions: '["5.1","5.2","5.3","5.4","5.5","luajit"]'
